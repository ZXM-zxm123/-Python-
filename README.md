# -Python-
使用 Python 标准库开发一个猜数字游戏。程序在 1-100 之间随机生成一个整数，玩家通过命令行输入猜测。每次猜测后提示“太大了”或“太小了”，猜中后显示猜测次数并询问是否继续。支持难度选择：简单（1-50）、中等（1-100）、困难（1-200）。将每个难度的最少猜测次数保存在本地文件中（如 best_scores.json），下次同难度超越时更新。提供退出选项。要求代码结构清晰，包含函数定义和主循环。
